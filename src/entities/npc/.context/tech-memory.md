# Npc Tech Memory

- Generated by `scripts/scaffold_catalog_entity.mjs`.
- T41 replaced the scaffold placeholder with a read-only catalog entity for training NPCs.
- The schema uses Drizzle-Zod/Zod with technical ids in English and visible labels in pt-BR.
- `NpcCatalogService` stays repository-agnostic, returns `Result`, and validates all repository output with `npcSelectSchema`.
- `factionId` is a technical reference to the existing training faction ids, but no foreign key, migration, or real persistence is created in T41.
- Rule sources: `docs/system/survival/06-npcs-e-aliados.md` and `docs/system/survival/regras-negociacao.md`.
