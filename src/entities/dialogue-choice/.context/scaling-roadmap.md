# Dialogue Choice Scaling Roadmap

- T49 can use these choices to decide which WorldState consequence is written after a social encounter ends.
- A future dialogue-tree phase should move from flat choices to authored nodes validated by `dialogue-architect`.
- Repeated use of the same `tag` should eventually interact with social fatigue, but T48 only stores the catalog data.
