# Scaling Roadmap

- Add optional dry-run support if users want preview diffs before writing memory.
- Add template versioning if the project memory protocol changes.
- Add module discovery against `src/features` after the feature tree becomes stable.
- Consider an allowlist of known modules if accidental feature creation becomes a risk.
- Add a companion tool to read existing module context summaries.
