# Plain English

This MCP server updates the three memory files that every Pandorha feature should keep.

You give it a module name, the errors found, a technical summary, and a plain-language explanation. It creates or updates the `.context` folder for that module.

It only writes inside `src/features`, so it cannot be used as a general file editor.
